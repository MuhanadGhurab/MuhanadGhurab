name: Validate profile

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Required files exist
        run: |
          set -euo pipefail
          test -f README.md
          test -f LICENSE
          test -f data/profile-status.json
          test -f data/github-evidence.json
          test -f scripts/render-profile-assets.py
          test -f scripts/generate_profile3_assets.py
          test -f scripts/update-github-evidence.py
          test -f tests/test_profile_assets.py
          test -f assets/profile/cyber-crow-hero-v2.svg
          test -f assets/profile/cyber-crow-hero-v2-static.svg
          test -f assets/profile/cyber-crow-operations.svg
          test -f assets/profile/conceptual-portfolio-architecture-v2.svg
          test -f assets/profile/portfolio-telemetry-v2.svg
          test -f assets/generated/github-evidence.svg
          test -f assets/generated/cyber-crow-contribution-flight.svg
          test -f assets/generated/cyber-crow-contribution-flight-static.svg
          test -f assets/profile/cyber-crow-command-center.svg
          test -f assets/profile/crow-mark.svg
          test -f docs/HERO-DESIGN-DECISION.md
          test -f docs/GITHUB-COMPATIBILITY.md
          test -f docs/BRAND-GUIDE.md
          test -f docs/PROFILE-CONTENT.md
          test -f docs/REFERENCE-STUDY-TROYMITCHELL911.md
          test -f docs/ANIMATION-REBUILD-DECISION.md
          test -f resume/README.md
          test -f resume/Muhanad-Ghurab-ATS-Resume.pdf
          test -f resume/Muhanad-Ghurab-ATS-Resume.docx
          test -f resume/Muhanad-Ghurab-ATS-Resume-preview.png
          test -f resume/source/resume-data.json
          test -f resume/ATS-CHECK.md
          test -f resume/tests/test_resume.py

      - name: Install resume validation deps
        run: pip install pypdf

      - name: Run profile asset tests
        run: python tests/test_profile_assets.py

      - name: Run resume ATS tests
        run: python resume/tests/test_resume.py

      - name: Secret pattern scan
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          import re
          patterns = [
              re.compile(r"AKIA[0-9A-Z]{16}"),
              re.compile(r"ghp_[A-Za-z0-9]{20,}"),
              re.compile(r"gho_[A-Za-z0-9]{20,}"),
              re.compile(r"xox[baprs]-[A-Za-z0-9-]{10,}"),
              re.compile(r"BEGIN (RSA|OPENSSH|EC) PRIVATE KEY"),
              re.compile(r"postgres(ql)?://[^\s]+:[^\s]+@"),
          ]
          skip = {".png", ".jpg", ".jpeg", ".gif", ".webp", ".pdf", ".docx"}
          hits = []
          for path in Path(".").rglob("*"):
              if not path.is_file() or path.suffix.lower() in skip:
                  continue
              if ".git" in path.parts:
                  continue
              try:
                  text = path.read_text(encoding="utf-8", errors="ignore")
              except OSError:
                  continue
              for pat in patterns:
                  if pat.search(text):
                      hits.append(f"{path}: matched {pat.pattern}")
          if hits:
              print("\n".join(hits))
              raise SystemExit(1)
          print("secret pattern scan clean")
          PY

      - name: Accuracy and resume-link gates
        run: |
          set -euo pipefail
          grep -q "Muhanad Ghurab" README.md
          grep -q "IT Specialist — Tekfen Construction" README.md
          grep -q "In Progress" README.md
          grep -q "cyber-crow-hero-v2.svg" README.md
          grep -q "profile-status.json" README.md
          grep -q "github-evidence.json" README.md || grep -q "github-evidence.svg" README.md
          ! grep -qi "Security+ Certified" README.md
          ! grep -qi "Certified PMP" README.md
          ! grep -qi "Aramco Employee" README.md
          grep -q "Muhanad-Ghurab-ATS-Resume.pdf" README.md
          grep -q "Muhanad-Ghurab-ATS-Resume.docx" README.md
          ! grep -q "OWNER INPUT REQUIRED" README.md
          ! grep -qi "Resume Pending" README.md
          test -f resume/Muhanad-Ghurab-ATS-Resume.pdf

      - name: Local asset references exist
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import re
          from pathlib import Path
          readme = Path("README.md").read_text(encoding="utf-8")
          refs = re.findall(r'\./(assets/[^)"\s]+)', readme)
          missing = [r for r in refs if not Path(r).exists()]
          if missing:
              raise SystemExit(f"missing assets: {missing}")
          print("asset refs ok", refs)
          PY
